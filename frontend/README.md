# Wandering Wallet Frontend

React + Vite. Config is the **repo-root** `.env` (`VITE_API_URL`, `VITE_BASE_URL`). See the main README.

```bash
npm install
npm run dev
```

http://localhost:5173 — backend must be running.

```bash
npm run build
```

Output: `dist/`.
