# Wandering Wallet Backend

Node.js + Express: auth, expenses, Google Sheets.

Config lives at the **repo root** (see the main README): `.env`, `app-config.json`, `google-service-account.json`.

```bash
npm install
npm run dev
```

Runs on the port in `.env` (default 3001).
